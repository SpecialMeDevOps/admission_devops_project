apiVersion: v1
kind: Service
metadata:
  name: admission-web
  namespace: production
spec:
  selector:
    app: admission-web
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: LoadBalancer
